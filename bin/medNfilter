#!/usr/bin/env lua
local tomoDir = os.getenv('TOMOAUTOROOT')
package.cpath = package.cpath .. ';' .. tomoDir .. '/lib/?.so;'
package.path = package.path .. ';' .. tomoDir .. '/lib/?.lua;'
local struct = require 'struct'
local tomoLib = require 'tomoLib'
local filename = arg[1]
local size = arg[2]
tomoLib.medNfilter(filename, size)
